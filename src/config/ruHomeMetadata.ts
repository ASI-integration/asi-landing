import type { Metadata } from 'next';

/** Shared RU homepage copy for both host-routed / and /ru. */
export const RU_HOME_METADATA: Metadata = {
  title: 'ASI сама ведёт рутину ваших объектов',
  description: 'ASI сама ведёт типовые задачи объекта. Подключение бесплатно, 14 дней после готовности — бесплатно. Затем 1 000 ₽ за объект в месяц, если решите продолжить.',
};
